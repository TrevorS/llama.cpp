# DS4 flag inventory (everything we introduced on this branch)

Single source of truth for every env gate and CLI flag added since the
upstream base (e3546c794): default, what ON/OFF does, where it's gated, and
validation status. Update this file in the SAME commit as any default change
or new flag. (Motivation: HC_FUSED sat silently off for a full bench
campaign; a missing FUSED_LID cost two crashed d65536 cycles.)

Audit method: `git diff e3546c794..HEAD | grep getenv` + added `add_opt`
CLI args. `LLAMA_DSV4_COMPRESS_DEBUG` is upstream (#24162), not ours.
`GGML_NO_BACKTRACE`/`GGML_BACKTRACE_LLDB` are upstream debug knobs.

Last audit: 2026-07-27 (tip 6f440a8f2 on upstream 1cbfd1988, post-rebase).
Cross-check method: every `LLAMA_*`/`GGML_*`/`DS4_*`/`CAPTURE_*` token in this
file grepped against ggml/ src/ common/ tools/. Result: 12 expected absences
(documented retirements/relocations below), 1 drift bug — `LLAMA_DSV4_MOE_GATE_FUSE`
consumed by upstream 846e991e during the 07-26 rebase, kill-switch restored
2026-07-27 (see its row). Findings #1–#3 (2026-07-19) postdate the previous
stamp and are recorded in the PDL / MOE_GATE_FUSE / LID_RADIX_DEC_MIN rows.

## Serving/bench-relevant flags

| Flag | Default | ON does | OFF does | Notes / validation |
| --- | --- | --- | --- | --- |
| `LLAMA_DSV4_FUSED_LID` | **ON** (`=0` disables, since 2026-07-14 P4a) | fused score+top-k op; O(chunk×nt) working set | unfused 8-op chain; O(n_ctx×nt×n_head) relu intermediate | **MANDATORY at depth** — unfused OOMs ≥ d65536 ub2048 (~8.6 GB relu). Token-identical (index-set + greedy gates). |
| `LLAMA_DSV4_FUSED_LID_TG_DEPTH` | 4096 | — | — | decode (nt=1) uses unfused below this n_lid (faster at bs1 shallow); 0 = always fuse, huge = never |
| `LLAMA_DSV4_HC_FUSED` | **ON** (`=0` disables, since 2026-07-14) | fused HC weighted_sum/post/sinkhorn kernels | scalar per-stream/unrolled chains (~16k-node decode graphs) | weighted_sum/post token-identical (gate 2); sinkhorn mode 21183→5439 nodes. Campaign refs always ran ON. |
| `LLAMA_DSV4_CSA_TILE` | **ON, W=16** (`=0` disables) | B2 per-tile union-gather CSA attention (prefill) | dense masked CSA FA | all gates passed (PPL, passkey 5/5, determinism). +12.2% pp@d65k → +61.9% @d262k IQ3. Self-gated: nt_s>W, n_stream==1, nt_s%W==0, n_csa≥TILE_MIN, 256-alignment |
| `LLAMA_DSV4_FA_SPLIT` | **ON** (`=0` disables) | tile branch runs split attention: dense raw-window FA (ne3=1, full width) + union-only tiled FA, both `ggml_flash_attn_ext_with_lse`, merged by `GGML_OP_DSV4_FA_MERGE`; no `repeat_4d` raw replication | single concat FA per tile (raw window physically replicated T×) | math-identical softmax over the same KV union (class C fp-reassociation); sink lives in the raw half's LSE. Only active inside the CSA_TILE branch with `-fa on` and T ≤ hd. Gates PASSED 2026-07-14: det, A/B byte-identical @42k, passkey 5/5, PPL within noise; +7.1% pp@d65k / +6.1% pp@d131k (new records), tg neutral. See PROGRESS iter 27. |
| `LLAMA_DSV4_CSA_TILE_UCAP` | 4096 | — | — | per-tile union cap; keep n_raw+u_cap 256-aligned; tail few % tiles truncate at d65k+ (passkey/PPL clean) |
| `LLAMA_DSV4_CSA_TILE_MIN` | 12288 | — | — | min n_csa to activate (~d49k); below it tiled FA loses (12 vs 41 TFLOPS small-nb latency wall) |
| `LLAMA_DSV4_CSA_GATHER` | **ON** (`=0` disables, since 2026-07-14 P4a) | B1 decode gather: attend raw window + 512 selected rows only | dense masked CSA FA at decode | +8.3% tg@d65k, grows with depth; decode FA share 2.5% @512k with it ON. First ~25 greedy tokens identical then fp-reassociation. |
| `LLAMA_DSV4_LID_RADIX` | **ON** (`=0` reverts to bitonic) | radix top-k on the f16 scores buffer: 2x8-bit MSB histogram threshold, ordered-scan compaction (all >T + lowest-index fill at ==T), one small bitonic on the top_k selected — one kernel/token replaces the chunk+merge tree | bitonic chunk+merge tree (SORT_N 4096) | Selection bit-identical to bitonic (-0 canonicalized so key-eq == f32-value-eq; ordered compaction, not atomic-append). Active for half scores (d128), n_lid>top_k, and **now DECODE too** (see DEC_MIN). Expected topk 50.4 to ~5ms @prod shape (op -37%). See PROGRESS iter 34/35. |
| `LLAMA_DSV4_LID_RADIX_DEC_MIN` | 4096 (`=SORT_N`) | **finding #3 (2026-07-19):** enable single-block radix at DECODE (nt<16) once `n_lid > DEC_MIN`. Radix is O(n_lid) vs bitonic chunk+merge O(n_lid·log²SORT_N), so on a wide decode row even one radix block crushes the bitonic tree — the old nt<16 gate needlessly kept the expensive path | below DEC_MIN, decode keeps bitonic single-chunk | **Validated: test-backend-ops matches CPU ref at the decode shape (n_lid=33280, nt=1); same proven kernel as prefill radix. Op-level topk 131→36µs (-73%) @d131k shape (whole indexer op 427→329µs, -23%); end-to-end tg128 +2.4% @d32k (grows with depth, ~+6% projected @d131k). A/B via `LLAMA_DSV4_LID_RADIX=0`.** Bit-identical selection (radix==bitonic). |
| `LLAMA_DSV4_LID_FP4_MMA` | **ON, DEPTH-GATED** (`=0` disables; Blackwell + container; auto-int8 above `_MAX_NLID`) | register-resident-K block-scaled fp4 tensor-core score kernel: reads 68B block_mxfp4 rows direct (bypasses staging + int8 pre-quant), mma.sync kind::mxf4 m16n8k64, q packed to e2m1 per head, f16 score store → radix | int8 dp4a score path | **MEASURED WIN 2026-07-15**: op 74.3→38.8ms @33280×2048×2048 (−47.8%, score kernel ~71→~35ms — register-resident K converts the L1 bound). Class B numerics (on the official e2m1 grid). Off-Blackwell → int8 (one-shot warn). **On-model depth A/B 2026-07-15 (both arms CACHE_MXFP4=1, POWER=85): d65k +4.7%, d131k +8.6% — advantage grows with depth. FLIPPED DEFAULT-ON 2026-07-16: PPL 4.2350 vs 4.2352 int8 (identical), passkey@42k 3/3, determinism byte-identical. DEPTH GATE same day (wedges #9-#11): sustained fp4-mma at the d131k shape hard-locks GB10 even under a perfect 75% duty cycle — burst draw 98.3W peak vs int8 83.0W (op-level, watchdog-guarded); duty shapes average power, the firmware latch is burst-sensitive. `LLAMA_DSV4_LID_FP4_MMA_MAX_NLID` (default 24576) falls back to int8 above the cap: keeps +4.7% @d65k, forfeits the unusable +8.6% @d131k. Telemetry: experiments/profiles/wedge-hunt/.** |
| `LLAMA_DSV4_LID_DEC` | **ON** (`=0` disables, since 2026-07-14 P4a) | dedicated nt=1 decode score kernel (warp-per-comp, int8) | 16-token-tile kernel with 15/16 padding waste at decode | 2.0x kernel, +5.2% tg (+10.8% with GATHER). Same int8 numerics class. |
| `LLAMA_DSV4_LID_CACHE_MXFP4` | **ON** (`=0` disables; also auto-off when `FUSED_LID=0`) | lid cache stored as packed MXFP4 (68 B/row vs 256 B f16, 3.76×); writes via `GGML_OP_DSV4_QAT_SET_ROWS` (QAT rounding in the scatter); decode reads 68 B rows directly, prefill stages to f16 (bit-exact); transitively engages fp4-mma at shallow depth | f16/f32 lid cache per `-ctk` | P3b. Rows are QAT by construction (the official post-QAT numeric). Forces fused lid at decode. State ver 2. Price: pp −2~3.6%, tg neutral @d131k. **FLIPPED DEFAULT-ON 2026-07-17**: 3 getenv sites (deepseek4 read+write, kv-cache-dsv4 alloc) compute identically — default-on, off if `=0` or `FUSED_LID=0` (graceful, no assert). Gates: op suite zero-tolerance, coherence/determinism, prompt-cache round-trip, perf legs iter 26; shallow on-model smoke (c8192, coherent, clean exit) 2026-07-17. |
| `GGML_CUDA_POWER_DEBUG` | OFF (`=1`) | governor telemetry at exit: computes / pairs measured / busy-skips / measured+slept ms / effective duty | — | stderr one-liner per device; proved duty accounting exact (85.0%, 0 skips) on both graph shapes 2026-07-16 |
| `GGML_CUDA_PDL` | **ON** (`=0` disables; upstream env var, we now consume it) | Programmatic Dependent Launch on our 9 DS4 **decode** kernels (dsv4_hc_fused: sinkhorn/weighted_sum/post; dsv4_lid_topk: score_decode/topk_single/union/memb/fa_merge/qat_set_rows) — `<<<>>>` → `ggml_cuda_kernel_launch` wrapper (`cudaLaunchKernelEx` + programmaticStreamSerialization) with device `ggml_cuda_pdl_sync()` before the first producer-data read; hides kernel launch latency in the launch-bound decode regime | plain launches (no overlap) | Prefill scorers (score/int8/fp4mma/prequant/chunk/merge/radix) intentionally NOT converted (compute-bound, launch latency amortized); upstream `lightning-indexer.cu` left verbatim (off our fused path). PDL⊥`__restrict__` → converted kernels drop restrict (matches norm.cu). **Validated 2026-07-19 (finding #1): PPL bit-identical three-way (baseline ≡ PDL=0 ≡ PDL=1 = 5.0177 @ c512/chunks100/-fa on/-ngl 999), test-backend-ops all 6 DSV4 ops OK, tg128@d0 +4.8% (17.85 vs 17.04), MTP gen +2.0% (25.29 vs 24.80, draft-accept byte-identical). Engages only ptxVersion≥90 (CUDA 13, sm_121a) — env kill-switch + PTX-cache guard.** |

| `LLAMA_DSV4_MOE_GATE_FUSE` | **ON** (`=0` disables) | fuse the DeepSeek-V4 `sqrt(softplus)` MoE gating chain into the existing `topk_moe_cuda` kernel — extends the fusion matcher (ggml-cuda.cu) to recognize the 2-op `SOFTPLUS→SQRT` gate (softmax/sigmoid are 1-op) + adds a `sqrt_softplus_warp_inplace` gate transform (replicates ggml `op_softplus`/`op_sqrt` exactly). Collapses ~11 unfused gating ops/layer (softplus/sqrt/reshape/add/argsort/view/get_rows/sum_rows/clamp/div/scale) → 1 kernel/layer (~473→43 launches/token — corrected 2026-07-27: DSV4-Flash has 43 layers (`block_count=43`), not 60; the measured +1.0% is unaffected, it was observed not derived) | unfused ggml op chain (the pre-existing path) | **Validated 2026-07-19 (finding #2): fuse=0 bit-identical to baseline (5.0177), fuse=1 = 5.0301 (+0.16σ, within noise — norm-sum warp-reduce reassociation; gate transform + expert selection are bit-identical, only the 6-weight sum reassociates). tg128@d0 +1.0% (18.04 vs 17.86), stacks with PDL. DSV4 has no expert groups (n_expert_groups=0) so the simple gate path fuses cleanly.** NOTE 2026-07-27: upstream 846e991e (#25896) absorbed kernel + matcher wholesale in the 07-26 rebase, silently turning `=0` into a no-op (drift caught by the 07-27 cross-check); kill-switch restored same day as an env guard on the matcher (`ggml_cuda_topk_moe_fusion`, SOFTPLUS→SQRT branch, ggml-cuda.cu) — our diff is now ONLY that guard. The +1.0% is baseline now, not our delta; `=0` stays the first-line PPL bisect lever (fuse=0 reproduces the bit-identical 5.0177-class path). |

| `GGML_CUDA_GRAPH_SHAPE_KEY` | **OFF** (`=1` enables) | folds an FNV hash of (n_nodes, per-node op + ne[4]) into the CUDA-graph cache key (`ggml_cuda_graph_get_key`) so shapes that alternate on the same first-node address get their own cache slots. Target: MTP draft-ctx churn — the draft alternates state-ingest (batch = n_accepted+1 ∈ {1,2,3}) with generate (batch 1) every round; on the stock nodes[0] key these shapes share slots → ~2 direct-execs + 1 re-capture per round (the D3-serving 526 resets/~1150 tok). Instrumented root cause (GGML_CUDA_GRAPH_DEBUG, 2026-07-27): 62/62 resets on the draft ctx, DIFF=ne on mtp_h_input/mtp_e_proj token dim, main verify ctx clean (its splits uid-fast-path every round). Key is map-only (never dereferenced); hash collision degrades to stock churn, never stale replay (property scan still guards); existing 10s eviction sweep bounds the map. | stock nodes[0] key | **PARTIAL VALIDATION 2026-07-27 (P95-graph A/B, 20-sample qualitative, same-boot): ON-greedy 25.40 vs OFF-greedy 24.89 = +2.0% t/s, acceptance IDENTICAL to 4 decimals in all 4 categories (0.7227/0.6937/0.6725/0.6207) — numerics untouched. OFF-temp1 23.46 (accept 0.6115) banked; ON-temp1 leg CRASHED at ~min 7 (the P95 accumulation wedge, see POWER row — attribution: power, not the flag; B1 ran the flag clean 5 min, and a capture/driver hang would present as a hung server with a live journal, not journal-stop). STILL OWED before default-ON: temp-1.0 arm + reuse-ratio verbose leg + PPL gate (≈5.0301 anchor) + determinism probe (the temp-0 probe returned empty content — reasoning_content extraction bug in the probe, not a model issue), all at P90 on the new boot. Follow-up (separate, upstream-relevant): the draft still REBUILDS its llama-level graph every pass-shape change (uid +3/build — can_reuse fails on token-count change); CPU-side build+sched-alloc cost untouched by this flag.** |

## Power / box-protection (ggml-cuda, 2026-07-15)

Duty-cycle governor targeting the GB10 pattern-#8 hard lockup (sustained deep prefill
engages firmware SW Power Cap which never clears → wedge). **2026-07-27 revision:
the depth-based trigger model is falsified — a #8-family wedge hit a SHALLOW c8192
MTP serving leg with ADAPT=1 active (see the ADAPT row's negative finding). Working
model is now burst structure: duty shapes average power, the firmware latch is
burst-sensitive (first established in the fp4-mma depth-gating analysis, iter 42).**
Idea from antirez/ds4
`--power N` (85% measured faster than 100% there under sustained load). Work intervals
measured with cudaEvent pairs per graph compute; compensating host sleep before the
next submit — no added syncs. Applies to every binary (bench/server/cli/tests).

| Flag | Default | Effect | Notes |
| --- | --- | --- | --- |
| `GGML_CUDA_POWER` | unset (=100, off) | fixed duty cycle N% in [1,99]: sleep `work×(100−N)/N` between graph computes | throttles avg board power to ~N% of sustained; ring of 8 event pairs + debt accumulator since 2026-07-16 (v1 single-pair proven exact but starvation-prone in principle); `GGML_CUDA_POWER_DEBUG=1` prints accounting at exit. **SURVIVAL STAMPED 2026-07-15: POWER=85 completed 2× consecutive pp2048@d131072 (262/264 t/s) — the sequence that wedged the box 3/3 times at 100%. MANDATORY on deep legs.** **SHALLOW SERVING SWEEP 2026-07-27 (attended, n=1/cell, c8192 MTP config): P90-graph 23.25 / P95-layer 24.87 / P95-graph 25.01 t/s vs P85 21.97 — duty→throughput 1:1, ALL CLEAN, accept byte-stable. Peak draw 77.8/83.6/91.6 W (85G/95-layer/95G) — layer granularity caps bursts at −0.6% tax. Deep legs stay at 85.** **P95 SERVING KILLED 2026-07-27 21:22 (wedge, pattern-#8 family): 4 back-to-back P95-graph shallow MTP serving legs (~23 min accumulated GPU-active over ~31 min, the shape-key A/B) wedged the box mid-leg-4 — journal-stop 21:22:17, zero OOM/Xid whole boot, mem 10%+ free, governor ledger exact 95.0% duty to the last telemetry line, then NUL-padded log tail (power-loss write). The morning sweep's 6-min P95 legs were under the accumulation threshold; ~23 min is over it. GRANULARITY=layer CANNOT rescue serving-95: serving is decode-dominated and decode replays are per-graph structurally (no host-sleep mid-replay) — the exposure is unreachable by chunked pacing. SERVING RECIPE: fixed 90, full stop; the "95 after soak" upgrade path is closed (the soak question answered itself, unstaged). Boot boundary: 14:24 boot ended 21:22; legs A1/A2/B1 of the shape-key A/B completed same-boot (valid); B2 partial = discarded per never-mix rule. **RETRACTION (coordinator round, same night): the day's "+13.8% serving" headline dies with P95 — the surviving measured gain is P85→P90 = 21.97→23.25 = +5.8% (P90-graph, sweep n=1). The claim was validated on 6-min legs, i.e. against an exposure profile below the wedge threshold — same failure class as the other retractions today. P95-measured numbers (24.87/25.01 sweep; 24.89/25.40/23.46 shape-key A/B) remain valid as same-boot DELTAS only, never anchors; P90-equivalents via the 1:1 relation (ON-greedy ~24.1, temp1 ~22.2) are PREDICTIONS pending the owed P90 legs. SERVING DOSE-RESPONSE (shallow decode-dominated exposure; deep fills excluded — the one-fill-per-boot latch fired even at duty 75): ADAPT/100%-bursts ~1h resident → wedge (07-26); fixed-95 ~23 min active → wedge (07-27); fixed-95 ~6 min → clean (07-27 sweep); fixed-90 long → UNTESTED; fixed-85 → most accumulated evidence but also NO formal long soak (many short legs + informal sessions). Any future long-session claim at 90 or 85 needs a staged soak with abort criteria — treat leg sequences as exposure tests until then.** |
| `GGML_CUDA_POWER_ADAPT` | OFF (`=1`) | closed loop: run at `GGML_CUDA_POWER` (or 100) until NVML distress, then drop to `GGML_CUDA_POWER_MIN` until clear 10 s | Linux-only (dlopen libnvidia-ml, no build dep); poll 500 ms; logs engage/clear. Distress = thermal-slowdown/power-brake bits always; SwPowerCap only with GPU util ≥50% (bit is benign at idle on GB10 — parked clocks set it). **NEGATIVE FINDING 2026-07-26 (logged 07-27): ADAPT=1 did NOT prevent a pattern-#8 wedge on a SHALLOW leg** — `-c 8192` MTP serving bench (session c4b77732), ~2 min into the MTP arm after ~1 h model-resident; journal-stop 15:25:47, no OOM/Xid, memory flat ~13 G avail; box down until power cycle 07-27 14:24. Falsifies depth-as-trigger. Mechanism hypothesis: MTP's draft/verify alternation is sub-graph-timescale burst variance that per-GRAPH granularity (ADAPT or fixed `GGML_CUDA_POWER=N`) samples too coarsely to smooth — a rerun that wedges at fixed 85 would confirm (same granularity). Next lever: per-layer granularity (`GGML_CUDA_POWER_GRANULARITY`, planned E2). Caveats: 07-26 recovery may not have been a true PD cold-drain, so a degraded-PD contribution isn't fully excluded (D0 07-27 passed healthy: 2333–2541 MHz, ≤99.9 W under load); pre-wedge speed-bench baseline 17.09 t/s discarded per the never-mix-legs-across-a-wedge rule. **CONFIRMATION RERUN 2026-07-27 (attended): the identical MTP serving config at FIXED `GGML_CUDA_POWER=85` (per-graph) completed 20/20 clean — peak 77.8 W, ≤80 °C, governor ledger exact both legs (85.0% duty, debt 0); the wedge does NOT reproduce when duty is capped the whole run (n=1; profiles/speedbench-20260727/). Reading: ADAPT with no `GGML_CUDA_POWER` runs 100% duty until NVML distress, and that after-the-fact floor is too slow for the burst latch — ADAPT-alone is the exposure. SERVING RECOMMENDATION until per-layer granularity (E2): fixed `GGML_CUDA_POWER=85`, not ADAPT-alone.** |
| `GGML_CUDA_POWER_MIN` | 60 | adaptive floor duty % | only read with ADAPT=1 |
| `GGML_CUDA_POWER_GRANULARITY` | `graph` (unset) | `layer` (=96 nodes) or `layerN`: chunked pacing on the DIRECT-EXEC path — every N launched nodes, sync the chunk span and sleep the duty complement inline, shortening the sustained-draw window the firmware latch sees (pp2048 window ~960 ms → ~69 ms at default N). Replayed decode CUDA graphs are structurally unaffected (cannot host-sleep mid-replay; per-graph pacing continues there — the proven-safe serving config). Chunk-paced computes suppress the whole-graph event pair (no double-tax); telemetry: `chunks/chunk-meas/chunk-slept/chunk-duty` in both DEBUG modes. | **E2, landed 2026-07-27. Shallow P85 validation: pp2048 437.74±0.81 vs 440.88±1.44 per-graph (−0.7% sync tax), tg128 14.98±0.07 ≡ 14.95±0.05 (decode untouched), chunk-duty exact 85.0% both legs, suppression verified (ring measured=0 on chunk-paced computes). VALUE LEGS PENDING (attended, wedge-hunt telemetry): P90/P95 shallow serving sweep (per coordinator arithmetic duty→throughput ≈1:1, P95 ≈ +12% serving — needs no new code, per-graph), then deep P85-layer survival, then `MAX_NLID` raise attempt at layer granularity. Chunk window scales with per-node time — tune `layerN` down (e.g. `layer48`) at depth. **FIRST-CLASS FINDING (P95 sweep, pre-registered): layer granularity's measured benefit is PEAK-DRAW reduction (83.6 vs 91.6 W @P95 serving), NOT throughput (−0.6%) and NOT demonstrated survival — the burst-wedge hypothesis is UNVALIDATED at depth (shallow legs differ only in prefill pacing; decode replays are per-graph structurally, so shallow cannot discriminate). Fresh-boot deep plan, in order, single-variable: (1) P85-layer survival pp2048@d131072, (2) MAX_NLID raise at layer granularity. P95 recipe upgrade requires a soak EXCEEDING the ~1 h 07-26 induction period — a 6-min leg is not a soak.** **RESOLVED 2026-07-27 21:22: the accumulation test ran itself — ~23 min of P95-graph serving wedged the box (see POWER row). P95 serving is closed at ANY granularity (decode replays are per-graph structurally); the staged SOAK-PLAN-20260727 at 95 is moot — if a soak still runs, it validates 90 for long sessions instead. E2 RE-SCOPED (coordinator round, precision fix on "inert for serving"): layer granularity is structurally inert for DECODE-phase exposure — where every serving wedge has occurred — because replayed graphs cannot host-sleep mid-replay; it remains ACTIVE for serving PREFILL (long-prompt ingest runs direct-exec and chunk-paces, e.g. 256k coding-agent sessions). Net: E2 = prefill-burst lever (bench prefill + serving prefill), no decode path, permanently. This also retro-explains the sweep's P95-layer 24.87 vs P95-graph 25.01: in decode those are the SAME execution; the 0.14 gap was prefill sync tax, not a discriminator.** |

## Speculative decoding / MTP (server + common)

| Flag | Default | Effect | Notes |
| --- | --- | --- | --- |
| `LLAMA_DSV4_SPEC_FRONTIER` | **ON** (`=0` disables) | the "frontier rewind" spec-decode rollback (server; code's own name for it) | part of the mtp-21.6 t/s recipe (d29e3b6); `atoi`, any non-zero = on |
| `LLAMA_MTP_FUSED_DRAFT` | OFF (set = on) | fused MTP draft chain (only when !chain_heads && !is_mem_shared) | opt-in; backend-sampling interaction untested — see speculative.cpp comment |
| `LLAMA_SPEC_TRACE` | OFF (set = on) | per-round SPECTRACE acc/draft log lines (server) | debug only |

## Refusal ablation / control-vector (llama-adapter + deepseek4)

| Flag | Default | Effect | Notes |
| --- | --- | --- | --- |
| `LLAMA_CVEC_ABLATE` | OFF (`=1` enables) | projection ablation `cur -= scale*<cur,dir>*dir` instead of additive steering | the working refusal recipe (bb2814b16): ablate + ffn-only + scale 2.0 |
| `LLAMA_CVEC_ABLATE_SCALE` | 1.0 | ablation strength | float |
| `LLAMA_CVEC_SCALE_FILE` | unset | live file re-read every graph build — runtime scale sweeps on one model load | added after the kill/relaunch hard-reset (crash memory #6) |
| `LLAMA_CVEC_AT_FFN` | OFF (set = on) | apply cvec at FFN (and attn unless FFN_ONLY) | |
| `LLAMA_CVEC_FFN_ONLY` | OFF (set = on) | restrict cvec to FFN — matches ds4.c exactly | required for the 100%-flip result |
| `CAPTURE_PROMPTS` / `CAPTURE_OUT` / `CAPTURE_OUT_THINK` / `CAPTURE_TENSOR` | unset | activation-capture tool env (prompt list, output paths, tensor to capture) | relocated to ~/Projects/ds4-refusal/refusal-capture (2026-07-16); the runtime CVEC ablation mechanism stays in-branch (llama-adapter.cpp + deepseek4.cpp) |

## Server CLI flags we added (pi-ds4-flash L2 KV tier, dcd503d94)

| Flag | Default | Effect |
| --- | --- | --- |
| `--cache-disk DIR` | unset (tier off) | enables the disk KV-state tier in DIR |
| `--cache-disk-mb` | 65536 | on-disk budget MiB (0 = unlimited) |
| `--cache-disk-min-tokens` | 2048 | smallest prompt worth persisting |
| `--cache-disk-max-entry-mb` | 4096 | largest single state diverted to disk |

## Debug/instrumentation flags (never in perf runs)

| Flag | Purpose |
| --- | --- |
| `LLAMA_DSV4_COMPRESS_DEBUG` | compressor debug prints |
| `GGML_SCHED_DEBUG=2` + `-lv 9` | full graph/split dump (node-count attribution) |
| `GGML_CUDA_GRAPH_DEBUG=1` | CUDA-graph churn diagnostics (2026-07-27): per-slot ctx/key/uid transitions + first differing node property (op/ne/nb/data_ptr/src\_\*) on every reset, PROPS_EQUAL on silent scan-reuse; warmup complete/reset lines carry ctx/key/uid/n_nodes. Found the MTP draft-ctx ingest/generate churn. Harness gotcha: this shell aliases grep→rg, which SILENTLY skips NUL-containing logs (verbose server logs qualify) — use `command grep -a`. |

## Retired / superseded

- **E8 DSpark draft evaluation — CLOSED BY PRIOR 2026-07-27.** Already ported, debugged, and tuned to its ceiling on this box (mxfp4 draft, swa-128, conf 0.60, in-graph chain); the 2026-07-09 12-cell matrix measured dspark 16.0/13.8 vs mtp 21.0/18.5 — draft-mtp dominates structurally (h-chained drafts track the quantized target; DSpark pays trained-in slot decay + parallel-draft drift). The 5.6 GiB draft also eats prefix-cache headroom. Nothing left to instrument.

- **E9 shared-expert side stream (K3 §5.2.1 pattern) — PARKED 2026-07-27, ceiling ~1–3%.** nsys node-trace of decode (32 tok, POWER=85, `profiles/e9-20260727/`): GPU busy 92–95% of the duty-allowed window; GEMVs = ~79% of active time (46 of ~58 ms/step) and E1 showed those run at the practical DRAM ceiling (Q8_0 control ~80% of theoretical too) — no idle bubble to fill, and the shared expert is itself pure DRAM traffic, so a side stream time-shares the same 273 GB/s. K3's win context is multi-GPU comm overlap (comm ≠ local DRAM); single-GPU unified memory is one bandwidth domain. Implementation hazards if ever reopened: ggml-alloc plans buffer reuse assuming SERIAL execution (concurrent streams can alias reused intermediates → corruption) + per-node stream routing in rebase-sensitive generic executor code. **Bonus profile (supersedes the 07-11 decode-stage split): post-PDL/fusion/graphs decode = GEMVs ~79%, small ops ~21%, graphs replaying, batch-1 routed experts run as `mul_mat_vec_q` with ids (~634 GEMV launches/step), `mul_mat_vec_q_moe` only at the 2..7-token verify shapes.**

- **E7 static-shape `mul_mat_id` (MoonEP pattern) — PARKED 2026-07-27 by D3.** Motivation was "graphs disabled by the mul_mat_id sync"; D3 measured plain decode replaying captured CUDA graphs on ~98% of steps (0 resets/128 tok in-window; 12 resets/600 tok = shape boundaries, self-healing; `profiles/d3-20260727/`). Batch ≤ 7 never trips [TAG_MUL_MAT_ID_CUDA_GRAPHS]. Only a >7-batch (prefill/MMQ) concern, where graphs matter little. PDL row reinterpretation: +4.8% is GPU-side dependent-launch overlap INSIDE replayed graphs, complementary to (not evidence against) graph replay. Open residual: MTP serving-mode reuse (n_tokens 3↔1 oscillation, the 07-11 "112 allow_reuse misses" datum) — count on a serving run.

- **E3 MTP margin confidence gate (ds4 `mtp_last_margin = v0 − v1`, antirez/main ds4.c:64001) — CLOSED BY EMPIRICAL PRIOR 2026-07-27, not implemented; theory argument RETRACTED same day.** Basis: the 2026-07-11 scout's confidence-truncation test (same intervention family, `--draft-p-min`) **measured −11.7% t/s end-to-end (22.2→19.6) with the gate predicting correctly** (mean_emit 2.33→2.45); acceptance regime unchanged since (0.676 today ≈ 0.65 then). **RETRACTION (verify-width byte law, K-depth session): my original "truncation saves ~ε" argument was WRONG — verify width multiplies distinct expert reads (see the structural entry below), so narrowing 3→2 saves ~2.1 GB/round (~16% of round bytes). A well-calibrated width gate has real byte room the closure rationale denied; the p_min gate lost despite it (probably classifier cost/calibration).** E3 therefore stays closed on the measured prior ALONE — weaker than originally stated; the reopen path is the ~30-line direct A/B of the margin statistic, now with a live mechanism it could exploit.

- **STRUCTURAL RESULT (2026-07-27, from the K-depth discriminator + corrected byte model): in a top-k MoE, deeper speculation is byte-negative because verify width multiplies distinct expert reads.** Per-token top-6-of-256 routing barely collides, so E[distinct experts/layer] ≈ 6.0/11.9/17.6/23.2 at widths 1–4 → expert bytes 2.24/4.43/6.56/8.65 GB while only dense+shexp (6.95 GB) amortizes. Accepted tokens grow sub-linearly (measured +16% K=2→3) vs ~+19% round bytes + an extra draft pass → measured −5.7%. Predicts the whole 2026-07-10 n_max curve with no free parameters. Consequences: K=2 is partly a function of the LARGE amortizable dense term — **dense requant (8.3→~5 bpw) shrinks the fixed term and pushes the width optimum DOWN; measure dense-requant × draft-depth together, not sequentially.** Dense-requant status: no quality test has ever run (the iter 10–12 "veto" was a policy line); **SOURCE CONFIRMED ON DISK 2026-07-27 (correcting an earlier "no source" claim): complete official checkpoint at `~/.cache/huggingface/hub/models--deepseek-ai--DeepSeek-V4-Flash/snapshots/60d8d707...` — 46/46 shards, FP8 e4m3 + ue8m0 128×128 block scales, 43 layers, ~156 GB.** Experiment path REVISED 2026-07-27 late (Unsloth third-party data): extract dense → mini-GGUF → surgery-merge into UD-IQ3 (experts bit-identical) → **first arm Q6_K dense only** (bracket, not 5 bpw — Unsloth's isolated dense experiment UD-Q8_K_XL→UD-Q4_K_XL = BF16→Q8_0 dense alone cost top-token 100→96.28%, KLD 0→0.0102, with DeepSeek's QAT stated as the reason dense is precision-sensitive; our dense is ALREADY Q8_0-class 8.33 bpw, i.e. at that measured point — 5 bpw is beyond all measured data on an unfavorable slope). Gates: **KLD vs our own UD-IQ3 base (`--kl-divergence-base`, isolates the dense delta exactly, runs on-box)** + PPL vs 5.0291 + acceptance as a REQUIRED TERM OF THE RESULT (the t/s prediction runs through the 1.48× multiplier at 0.676 — if acceptance moves, the predicted gain is wrong) with CALIBRATED reach as a quality signal: the 07-11 flat-acceptance prior varied EXPERTS; dense-side is untested and mechanistically riskier (MTP head and output head are different functions of the same degraded trunk input, trained against the unquantized state distribution — coherence need not hold). KLD-vs-own-base caveat: measures MARGINAL movement from an already-degraded base (~2.76 bpw experts), not absolute distance from official; errors may compose non-linearly; scale reference = Unsloth's BF16→Q8_0-dense step at KLD 0.0102 — a Q6_K arm materially above that is a bigger perturbation than any published step. Absolute grounding if the bracket looks good: CPU transformers logit-diff on a few hundred tokens vs the FP8 snapshot anchors the local scale + tg@P85; jointly with draft-depth K per the width law. Counter-consideration keeping the experiment alive despite the slope: Unsloth optimizes KLD per FILE GB; we need t/s per RUNTIME GB — dense bytes cost ~43× expert bytes at runtime (read every token vs 6/256), an objective their allocation cannot see. **IQ2 direction RETIRED with third-party numbers: antirez IQ2XXS measures PPL 6.15 vs official 4.53, top-token 77.9% (Unsloth wikitext-2 c512) — Teej's quality refusal was right.** Serving-harness hygiene: no `-ctk/-ctv` quant in our legs (PR #25202 KV-quant issue moot); speed-bench temp 0 is deliberate for deterministic A/Bs, Unsloth's temp-1.0 applies to qualitative evals only. **PRODUCTION-SAMPLING CHECK 2026-07-27 night: MTP engages at any temp (no greedy gate; sampled acceptance via common_sampler_sample_and_accept_n); measured temp-1.0/top-p-1.0 @P95: 23.20 t/s, accept 0.596 vs greedy 25.01/0.676 (−7.2% t/s) — still ~+40% over no-MTP; greedy numbers are the A/B instrument, temp-1.0 numbers are the production figures. D3-serving: MTP mode is a PARTIAL graph-replay regime (526 resets/536 completes/2350 reuses per ~1150 tok — shape alternation churn); shape-stable draft/verify graphs = unscheduled ~2–5% target.** (ds4 "no explicit -arch is fastest on GB10") — evaluated 2026-07-27, WASH (negative, no change adopted).** Arm A = our standard `CMAKE_CUDA_ARCHITECTURES=121a` (SASS cubins + PTX in fatbin; driver runs the SASS). Arm B = `121a-virtual` (PTX ISA 9.0 only, driver 580.159.03 JIT vs toolkit ptxas 13.0.88 — genuinely different compilers). Same-boot interleaved llama-bench @ fixed POWER=85 (duty exact 85.0% all legs, `profiles/e4-20260727/`): pp2048 440.88±1.44 (A) vs 441.73±0.67 (B) = +0.2%; tg128 14.95±0.05 (A) vs 14.85±0.14 (B) = −0.7% — both within noise. PDL survived JIT (per-kernel `attr.ptxVersion>=90` gate passes on ISA 9.0; a PDL loss would read ≈−4.8% tg, absent). JIT tax: one-time ~18 s on first cold-cache load, then ComputeCache absorbs it. **Keep explicit `121a`: no JIT/ComputeCache dependence at equal speed. The ds4 README claim doesn't transfer to this kernel set — a true no-arch nvcc default build would compile OUT the Blackwell paths (fp4-mma, PDL) and measure a downgrade, not codegen.** Repro: `cmake -B build-e4 -DGGML_CUDA=ON -DCMAKE_BUILD_TYPE=Release -DCMAKE_CUDA_ARCHITECTURES=121a-virtual` (tree kept at `build-e4/`).

- **E1 "adaptive expert tile width" (ds4 port) — evaluated 2026-07-27, NOT APPLICABLE (negative result, no flag ever added).** ds4's auto tile4-at-n≤8 (`antirez/main ds4_cuda.cu:20941`) halves the empty dot-slots of ds4's OWN sorted 8-slot expert tiles; upstream's `mul_mat_vec_q_moe` (mmvq.cu:707, owns n ≤ mmvq_mmid_max = 7) is warp-per-(slot,token) — no tile, no empty slots to halve, exact by construction. Measured (test-backend-ops perf, DSV4 shapes 256 experts top-6, gate/up 2048×4096 + down 4096×2048, full power, `speedbench-20260727/e1-mmid-perf.txt`): elapsed perfectly ∝ pairs from n=2→7 (IQ3_XXS ≈16.5 µs/pair both shapes); streaming at n≥3 = IQ3_XXS 194–207 / IQ2_XXS 215–238 / Q8_0 211–236 GB/s ≈ 72–86% of the 273 peak; sub-byte penalty vs Q8_0 only ~5–10% — NO LID_INT4-style unpack cliff at these 8.4M-weight row shapes. Dedup ceiling ~7% @n=7 (256-expert routing barely collides; matches the nt=4 → 24-unique measurement). **Doubles as the D4 answer for decode/verify: bandwidth-bound near roofline → E5 (offline layout permutation) and E6 (lane teams) are de-prioritized for the decode path; the tg roofline gap (~38% of ceiling) lives in launch/graph overhead, not the expert GEMM.** Caveat: n=1–2 harness numbers are L2-inflated (repeat-run residency reads >273 GB/s apparent) — trust n≥3. The n_mats=256 perf sweep is committed in test-backend-ops for re-measurement. **D4 REFERENCE NAILED 2026-07-27 (same-bytes sequential dense-GEMV controls, m=86016, committed alongside): practical sequential ceiling on GB10 = 221 (IQ3_XXS) / 235 (IQ2_XXS) / 228 (Q8_0) GB/s = 81–86% of the unreachable 273 spec; gather tax at the n=7 shape ≈ 7% (Q8_0 213 vs 228) → expert-placement/locality levers have a ~3% end-to-end ceiling (7% × ~47% traffic share) — E5-class work stays de-prioritized WITH a direct control now. IQ3 sequential unpack tax vs Q8_0 ≈ 3% (compute overlap hides it). Quant-bytes mechanics confirmed (IQ2 streams fastest, 235); the IQ2-vs-IQ3 model choice stays quality-gated per the 07-11 4-cell (+7–8% t/s for IQ2, declined; MTP shrinks the IQ3 penalty to ~3–6.5%).**

- `LLAMA_DSV4_CSA_UNION`, `LLAMA_DSV4_CSA_UNION_CAP`, `LLAMA_DSV4_CSA_UNION_FULL`
  — whole-batch union path, replaced by `LLAMA_DSV4_CSA_TILE` (iter 18d/19).
- `LLAMA_DSV4_LID_INT4` — negative result (compute-bound on nibble→int8 expansion, 1.6x slower than int8), reverted (iter 15).
- `LLAMA_DSV4_LID_INT8` — flag retired 2026-07-17: int8 dp4a is now the unconditional d128 non-fp4 score path; the wmma128 fallback kernel it A/B'd against was removed (superseded, only reachable with `=0`).
- `LLAMA_DSV4_LID_DUMP` / `_NLID`, `LLAMA_DSV4_UNION_STATS`, `LLAMA_DEBUG_NEXTN` — debug/oracle instrumentation removed 2026-07-17 (minimal-diff refinement pass).

## Canonical bench/serving env

As of P4a (2026-07-14) the fast profile IS the default — no env needed:
FUSED_LID, CSA_GATHER, LID_DEC, CSA_TILE, HC_FUSED all ON (int8 dp4a scoring
is unconditional since 2026-07-17). Since 2026-07-17 LID_CACHE_MXFP4 is also
default-ON (packed lid container; also engages fp4-mma at shallow depth) —
set `=0` to fall back to the f16 lid cache.

Pre-P4a baseline reproduction (legacy flags off; `LID_INT8=0` retired — the
int8 score path can no longer be disabled):

```
LLAMA_DSV4_FUSED_LID=0 LLAMA_DSV4_CSA_GATHER=0 LLAMA_DSV4_LID_DEC=0
```
